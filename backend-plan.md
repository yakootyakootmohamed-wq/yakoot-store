# خطة النسخة الإنتاجية YAKOOT

1. إنشاء مشروع Supabase مجاني.
2. تشغيل ملف `supabase_schema.sql`.
3. إنشاء Storage bucket للصور باسم `products`.
4. وضع Project URL وAnon Key في إعدادات التطبيق.
5. ربط لوحة الإدارة بالمصادقة، بحيث لا يستطيع أي عميل رؤية الطلبات.
6. ربط إنشاء الطلبات بقاعدة البيانات.
7. إضافة إشعار واتساب/بريد عند وصول طلب جديد.
8. بعد اختبار الطلبات، تغليف الموقع كتطبيق Android.

## ملاحظة
النسخة الحالية لا تحتوي على مفاتيح Supabase؛ لا يتم وضع مفاتيحك السرية داخل ملفات الواجهة.
